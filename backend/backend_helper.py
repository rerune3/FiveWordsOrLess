from proto.topic_proto import TopicRequestProto
from proto.topic_proto import TopicResponseProto

class BackendHelper:

    @staticmethod
    def topic_response_results_to_proto_list(responses):
        arr = []
        for response in respones:
            arr.append(response)
        return arr
